{
  "name": "porsche-showcase",
  "version": "1.0.0",
  "private": true,
  "description": "A Porsche showcase site — vanilla HTML/CSS/JS frontend, Express + SQLite API.",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "npm -w server run dev",
    "build": "npm -w server run build",
    "seed": "npm -w server run seed",
    "start": "npm -w server run start"
  }
}
